//栈 先进后出		队列 先进先出 
#include<iostream> 

using namespace std;

const int N = 100010;

//********************栈 *********************
int stk[N], tt;

//插入
stk[++tt] = x;

//出栈
tt--;

//判断栈是否为空
if(tt > 0) not empty;
else empty;

//栈顶
skt[tt];

//*******************队列*********************
//在队尾插入元素，在队头弹出元素
int q[N], hh, tt = -1;

//插入元素
q[++tt] = x;

//弹出
hh++;

//判断队列是否为空
if(hh <= tt) not empty
else empty

//取队头元素
q[hh]

 









 
